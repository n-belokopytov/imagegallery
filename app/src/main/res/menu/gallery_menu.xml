<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/menu_list"
        android:title="@string/gallery_menu_list"
        android:icon="@drawable/ic_grid_list"
        android:visible="false"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/menu_grid"
        android:title="@string/gallery_menu_grid"
        android:icon="@drawable/ic_grid_menu"
        android:visible="true"
        app:showAsAction="ifRoom" />
</menu>